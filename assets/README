The files here are generated by
- `go-bindata -i materials.json -p="assets" -f="Materials_json"
- `go-bindata -i item_names.json -p="assets" -f="Item_names_json"
where
- materials.json is the output of import_materials.rb
- item_names.json is the output of import_item_names.rb
